- [x] **Execution context**: Luôn chạy lệnh từ thư mục `frontend/`
- [ ] Fix Jest TypeScript config loading (remove ts-node requirement)
- [ ] Re-run `cd frontend && npm test -- --runInBand` to ensure test globals/types are recognized
- [ ] Verify Table.test.tsx expectations match Table.tsx behavior
*Note: Nếu chạy từ root, sử dụng `npm test -w frontend` (nếu có npm workspaces).*
